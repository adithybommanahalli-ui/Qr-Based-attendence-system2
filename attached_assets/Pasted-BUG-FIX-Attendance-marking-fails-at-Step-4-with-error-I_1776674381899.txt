BUG FIX: Attendance marking fails at Step 4 with error "Invalid QR code" even though the session is active and the QR was just generated by the lecturer.

ROOT CAUSE ANALYSIS — investigate and fix ALL of the following possible causes:

1. QR CONTENT MISMATCH
   - When generating the QR in /api/session/start, the `qrcode` library may be encoding the ENTIRE URL or a JSON object, but the backend /api/attendance/mark is looking up Session.qr_token by RAW token string only.
   - FIX: Ensure QR encodes ONLY the raw qr_token string (e.g., the uuid4 hex). Example:
        img = qrcode.make(session.qr_token)   # encode ONLY the token, nothing else
   - In the scanner frontend (mark_attendance.html / qr-scanner.js), pass the decoded text DIRECTLY as qr_token without modification:
        fetch('/api/attendance/mark', { body: JSON.stringify({ qr_token: decodedText.trim(), ... }) })
   - Add `.trim()` to remove whitespace/newlines from the scanned string.

2. BACKEND LOOKUP BUG
   - In /api/attendance/mark, check the lookup line. It should be:
        session = Session.query.filter_by(qr_token=data['qr_token']).first()
     NOT filter_by(id=...) and NOT with any extra conditions that might exclude it.
   - If session is None → return "Invalid QR code".
   - Add a debug print BEFORE the check:
        print(f"[DEBUG] Received qr_token: '{data.get('qr_token')}'")
        print(f"[DEBUG] All active tokens: {[s.qr_token for s in Session.query.all()]}")

3. TOKEN STORAGE BUG
   - Verify that when /api/session/start creates the Session, it commits `qr_token` to the DB BEFORE returning the response. Some implementations generate the token, build the QR image, return the response, but forget db.session.commit().
   - FIX: Ensure order is:
        session = Session(qr_token=str(uuid.uuid4()), ...)
        db.session.add(session)
        db.session.commit()          # commit FIRST
        # then generate QR from session.qr_token
        # then return response

4. FRONTEND STATE BUG
   - In mark_attendance.html, make sure the scanned qr_token is stored in a variable (e.g., window.scannedToken) and actually sent in the final fetch. Log it:
        console.log('Submitting qr_token:', scannedToken);
   - Ensure the variable is NOT reset between steps.

5. DOUBLE-ENCODING BUG
   - If you encoded the QR with a prefix like "ATTENDANCE:<token>" or a JSON, update backend to strip it OR (preferred) re-generate QR with raw token only.

ACTION — APPLY THESE FIXES:

A. Update /api/session/start to encode ONLY the raw qr_token in the QR image:
   ```python
   import qrcode, io, base64, uuid
   token = str(uuid.uuid4())
   session = Session(qr_token=token, section_id=..., start_time=..., end_time=..., status='active', latitude=..., longitude=..., radius_meters=...)
   db.session.add(session)
   db.session.commit()
   img = qrcode.make(token)   # RAW TOKEN ONLY
   buf = io.BytesIO()
   img.save(buf, format='PNG')
   qr_b64 = 'data:image/png;base64,' + base64.b64encode(buf.getvalue()).decode()
   return jsonify({ 'success': True, 'data': { 'session_id': session.id, 'qr_token': token, 'qr_image': qr_b64, 'end_time': session.end_time.isoformat() } })